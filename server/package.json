{
  "name": "zamma-server",
  "version": "1.0.0",
  "description": "Zamma Backend — lokale Hobby-Verabredungen (Spez. siehe Design-Handoff README)",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.js",
    "dev": "node --env-file-if-exists=.env --watch src/index.js",
    "test": "node --test test/",
    "db:ping": "node --env-file-if-exists=.env scripts/db-ping.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "pg": "^8.22.0",
    "ws": "^8.18.0"
  }
}
